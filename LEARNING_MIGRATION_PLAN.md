# Health 项目渐进式搬运与学习计划

## 1. 计划目标

本计划用于将同级目录 `health-old` 中的代码，逐步学习并迁移到当前 `health` 目录。

这不是一次完整复制任务。每次只实现一个规模较小、能够独立运行和验证的功能，通过“阅读、理解、实现、测试、总结”的循环掌握项目。

最终目标：

- 理解 Vue 3 前端项目的启动、路由、页面、状态管理和接口调用。
- 理解 Spring Boot 后端的 Controller、Service、Mapper 和数据库分层。
- 掌握登录认证、人员、设备、健康指标和风险预警等主要业务链路。
- 理解 Redis 缓存、异步批量入库、Netty TCP 手表协议等进阶功能。
- 能够不依赖整段复制，独立修改、测试和排查项目。

## 2. 项目范围

旧项目位置：

```text
/Users/jiangqianli/Documents/code/health-old
```

学习项目位置：

```text
/Users/jiangqianli/Documents/code/health
```

旧项目主要组成：

```text
health-old/
├── HealthShow/                    Vue 3 前端
├── HealthData/                    Spring Boot 后端
├── firmware/esp32c3-wifi-watch/  ESP32-C3 手表原型
└── tools/                         启动、数据库和协议辅助工具
```

## 3. 搬运原则

### 3.1 一次只搬一个可验证的小功能

不要先复制完整前端，再复制完整后端。应当按一条完整业务链路学习，例如：

```text
Vue 页面
  → 前端 API 文件
  → HTTP 请求
  → Controller
  → Service
  → Mapper
  → SQL Server
```

完成并理解一条链路后，再进入下一条。

### 3.2 先自己写最小版本，再参考旧代码

每个功能先尝试写一个最简单的版本。遇到问题时，再到 `health-old` 查找对应实现。不要一开始就复制完整文件。

### 3.3 项目必须始终可以运行

每完成一个小步骤，立即运行构建或测试。不要积累几十个文件后才第一次启动。

### 3.4 新旧项目共用旧数据库时必须保护现有数据

- `health-old` 与当前 `health` 学习项目都使用现有数据库 `health`。
- 新项目只通过本地环境变量或本地配置连接 `health`，数据库账号、密码和地址不得提交到 Git。
- 不通过请求头、Cookie 或页面开关动态切换数据库。
- 不要把旧项目的数据库连接配置和密码直接复制到可提交的配置文件中。
- 未明确确认影响范围并做好备份前，不执行删库、删表、清空数据或不可逆的表结构变更。
- 自动化测试不得清空或覆盖旧库中的现有业务数据；需要写入时使用可识别、可单独清理的测试数据。
- 两套项目同时运行时，应避免前端、后端、Redis 和 TCP 端口冲突。

### 3.5 每个阶段单独提交

建议一个可运行的里程碑对应一个 Git commit，例如：

```text
chore: initialize vue frontend
chore: initialize spring boot backend
feat: connect frontend to hello api
feat: add department query
feat: add department management
```

## 4. 禁止直接搬运的内容

以下内容是依赖、缓存、构建结果、日志或旧运行环境产物，不复制到新项目：

```text
health-old/.git
HealthShow/node_modules
HealthShow/dist
HealthShow/.vite
HealthShow/outputs
HealthData/target
HealthData/runtime
runtime-logs
.idea
.DS_Store
历史版本
```

以下内容只能阅读后重新配置，不能原样复制：

- `.env.development`、`.env.production`、`.env.staging`
- `application.yml` 中的数据库账号、密码和地址
- 强制连接旧数据库的启动脚本
- 包含本机路径、令牌、Wi-Fi 密码或其他凭据的配置

## 5. 每个功能的标准学习循环

每个功能都按照以下步骤执行：

1. 明确功能解决的业务问题。
2. 在旧项目中找到入口文件和调用链。
3. 画出简短的数据流或调用关系。
4. 列出本次真正需要的文件和依赖。
5. 在新项目中手动实现最小版本。
6. 编译并修复缺失依赖。
7. 手动操作或使用 `curl` 验证。
8. 添加至少一个正常场景测试。
9. 添加至少一个错误场景测试。
10. 用自己的语言写学习总结。
11. 检查 Git diff 并提交当前里程碑。

## 6. 分阶段实施计划

### 阶段 0：准备学习环境

目标：建立安全、清晰、可重复的开发环境。

任务：

- [ ] 确认 Java 21、Maven、Node.js 20.19+、npm 和 Git 可用。
- [ ] 确认当前 `health` 目录与 `health-old` 相互独立。
- [ ] 在当前目录初始化 Git 仓库（如果尚未初始化）。
- [ ] 创建适合 Java、Node.js 和 macOS 的 `.gitignore`。
- [ ] 确定新项目复用现有数据库 `health`，并记录共享数据库的数据保护约束。
- [ ] 建立个人学习记录目录或笔记文件。
- [ ] 记录常用的启动、停止和检查命令。

验收标准：

- [ ] `java -version` 显示 Java 21。
- [ ] `mvn -version`、`node -v`、`npm -v` 能正常执行。
- [ ] `git status` 能在当前项目中正常运行。
- [ ] 新项目没有引用 `health-old/.git`。

### 阶段 1：建立最小 Vue 前端

目标：理解 Vue 应用从 HTML 到页面显示的启动过程。

任务：

- [x] 创建新前端项目目录 `HealthWeb`（旧项目参考目录仍为 `health-old/HealthShow`）。
- [x] 创建最小 `package.json`。
- [x] 安装 Vue 3、Vue Router、Vite 和 Vue Vite 插件。
- [x] 创建 `index.html`。
- [x] 创建 `src/main.ts`。
- [x] 创建 `src/App.vue`。
- [x] 创建一个首页组件。
- [x] 配置一个最简单的路由。
- [x] 添加开发和构建脚本。

此阶段暂时不要加入：

- Element Plus
- Vuex
- ECharts
- Axios
- PWA
- 完整后台布局
- 旧项目全部页面

验收标准：

- [x] `npm run dev` 能启动前端。
- [x] 浏览器能看到“Health 学习版”首页。
- [x] 页面路由可以正常切换。
- [x] `npm run build` 成功。
- [x] 能解释 `index.html → main.ts → App.vue` 的关系。

### 阶段 2：建立最小 Spring Boot 后端

目标：理解 Spring Boot 的启动类、Controller 和 HTTP 响应。

任务：

- [x] 创建新后端项目目录 `HealthApi`（旧项目参考目录仍为 `health-old/HealthData`）。
- [x] 在 `pom.xml` 中只加入 Spring Web、Actuator 和测试依赖。
- [x] 创建 `HealthApplication` 启动类。
- [x] 创建 `/hello` 接口。
- [x] 配置 `/health` 作为应用上下文路径。
- [x] 配置 Actuator 健康检查。
- [x] 为 `/hello` 添加一个 Controller 测试。

此阶段暂时不要加入：

- SQL Server
- MyBatis-Plus
- Redis
- Sa-Token
- Spring Security
- Netty
- WebSocket

验收标准：

- [x] `mvn test` 成功。
- [x] `mvn spring-boot:run` 能启动后端。
- [x] `/health/actuator/health` 返回 `UP`。
- [x] `/health/hello` 返回预期 JSON。
- [x] 能解释启动类和 Controller 的作用。

### 阶段 3：打通前后端第一次请求

目标：理解页面、Axios、Vite 代理和后端接口之间的关系。

任务：

- [x] 安装 Axios。
- [x] 创建简化版 `src/utils/request.ts`。
- [x] 创建 `src/api/hello.ts`。
- [x] 配置 Vite `/dev-api` 代理。
- [x] 在首页添加“调用后端”按钮。
- [x] 显示加载、成功和失败状态。
- [x] 使用浏览器开发者工具查看请求与响应。

验收标准：

- [x] 点击按钮能够显示后端返回的数据。
- [x] 后端停止时，页面能显示明确的错误提示。
- [x] 浏览器请求通过 Vite 代理到达后端。
- [x] 能解释跨域、代理和基础 URL 的作用。

### 阶段 4：数据库与部门管理

目标：完成第一个真正的前后端数据库 CRUD 功能。

任务：

- [x] 确认现有 SQL Server 数据库 `health` 可访问。
- [x] 添加 SQL Server 驱动。
- [x] 添加 MyBatis-Plus。
- [x] 在 `application.yml` 中配置数据库连接。
- [x] 阅读并记录现有部门表结构，不直接执行破坏性迁移。
- [x] 创建 Department Entity。
- [x] 创建 Department Mapper（只读列表查询）。
- [x] 创建 Department Service（只读列表查询）。
- [x] 创建 Department Controller（只读列表接口）。
- [x] 创建前端 Department API。
- [x] 创建部门列表页面。
- [ ] 实现新增、修改和删除。
- [ ] 添加输入校验和错误处理。
- [ ] 添加后端 CRUD 测试。

验收标准：

- [x] 部门列表来自现有 `health` 数据库。
- [ ] 新增、修改和删除均可正常完成。
- [ ] 非法输入不会写入数据库。
- [ ] 页面刷新后数据仍然存在。
- [ ] 能解释 Entity、Mapper、Service、Controller 的职责。

### 阶段 5：登录、用户、角色和权限

目标：理解认证与授权的区别，并保护业务页面和后端接口。

建议顺序：

1. 用户表和用户查询。
2. 用户名密码登录。
3. 后端生成 Token。
4. 前端保存 Token。
5. Axios 自动携带 Token。
6. 路由守卫。
7. 退出登录。
8. 角色管理。
9. 权限管理。
10. 根据权限过滤菜单。

任务：

- [ ] 创建用户、角色、权限及关联表。
- [ ] 加入 Sa-Token。
- [ ] 创建登录和退出接口。
- [ ] 创建登录页面。
- [ ] 创建 Token 工具。
- [ ] 创建 Axios 请求拦截器和响应拦截器。
- [ ] 创建前端路由守卫。
- [ ] 保护部门管理接口。
- [ ] 实现最小角色和权限管理。
- [ ] 测试未登录、登录成功和权限不足场景。

验收标准：

- [ ] 未登录用户不能访问部门管理。
- [ ] 登录成功后可以访问授权页面。
- [ ] Token 失效后能够回到登录页。
- [ ] 退出后旧 Token 不再可用。
- [ ] 能解释认证和授权的区别。

### 阶段 6：岗位、人员与设备

目标：建立健康数据依赖的核心业务对象。

建议顺序：

1. 岗位管理。
2. 职工管理。
3. 设备管理。
4. 职工与设备绑定。
5. 根据 IMEI 查询设备和职工。

任务：

- [ ] 完成岗位 CRUD。
- [ ] 完成人员 CRUD 和分页查询。
- [ ] 完成设备 CRUD 和状态显示。
- [ ] 建立人员、部门和岗位关系。
- [ ] 建立人员与设备绑定关系。
- [ ] 对 IMEI 增加格式校验和唯一性约束。
- [ ] 添加前后端测试。

验收标准：

- [ ] 能新增职工并指定部门、岗位。
- [ ] 能新增设备并绑定职工。
- [ ] 一个有效 IMEI 不会被重复绑定。
- [ ] 能根据职工查设备，也能根据设备查职工。

### 阶段 7：第一种健康指标——心率

目标：掌握一条健康数据从数据库到图表的完整链路。

任务：

- [ ] 创建心率记录表。
- [ ] 创建 Entity、Mapper、Service 和 Controller。
- [ ] 实现按人员和时间范围查询。
- [ ] 实现服务端分页。
- [ ] 创建前端心率 API。
- [ ] 创建心率列表页面。
- [ ] 加入 ECharts 并绘制心率趋势。
- [ ] 显示空数据、加载和请求失败状态。
- [ ] 添加接口和页面测试。

验收标准：

- [ ] 可以写入一条心率数据。
- [ ] 可以按人员和日期范围查询。
- [ ] 分页总数来自后端。
- [ ] 图表展示真实接口数据。
- [ ] 页面不使用虚构数据冒充真实数据。

### 阶段 8：扩展其他健康指标

目标：通过重复同一种架构，加深对业务分层的理解。

按照以下顺序逐个实现，每次只做一种：

- [ ] 血压。
- [ ] 血氧。
- [ ] 体温。
- [ ] 压力。
- [ ] 睡眠。

每一种指标都必须完成：

- 数据库表或字段
- Entity
- Mapper
- Service
- Controller
- 前端 API
- 页面或图表
- 正常场景测试
- 空数据和错误场景测试

验收标准：

- [ ] 每一种指标均能独立写入和查询。
- [ ] 页面时间、单位和状态显示正确。
- [ ] 能指出各指标相同与不同的实现部分。
- [ ] 没有通过复制粘贴留下错误字段名或单位。

### 阶段 9：风险阈值与预警生命周期

目标：理解业务规则如何从健康数据产生风险事件。

任务：

- [ ] 创建阈值配置。
- [ ] 根据人员和指标读取有效阈值。
- [ ] 判断健康数据是否越界。
- [ ] 生成结构化预警记录。
- [ ] 区分健康阈值、设备报警和趋势预警。
- [ ] 创建预警列表和详情。
- [ ] 实现确认、分派、处理、关闭和误报。
- [ ] 保存状态变化时间线。
- [ ] 添加预警生命周期测试。

业务规则：

- 高危健康记录不等于 SOS。
- SOS 只代表设备主动上报的求救事件。
- 已确认、已处理和已关闭是不同状态。
- 前端不能用修改本地数组冒充后端状态已更新。

验收标准：

- [ ] 正常数据不会生成预警。
- [ ] 越界数据按配置生成预警。
- [ ] 预警的来源、代码和严重程度含义清楚。
- [ ] 状态变化能够从数据库重新读取。

### 阶段 10：Redis 与实时监控

目标：理解缓存、数据新鲜度、在线状态和异步处理。

建议顺序：

1. Redis 连接和简单读写。
2. 设备在线集合。
3. 人员实时健康快照。
4. 数据新鲜度。
5. Redis List 缓冲。
6. 定时批量写入 SQL Server。
7. 失败重试和 dead-letter。
8. Redis 故障时回退数据库。

任务：

- [ ] 使用独立的新项目 Redis key 前缀或后缀。
- [ ] 实现实时快照读取。
- [ ] 实现在线窗口和数据新鲜度判断。
- [ ] 实现 `normal`、`warning`、`stale`、`no_data` 状态。
- [ ] 实现有界异步执行器。
- [ ] 实现批量写库和失败处理。
- [ ] 创建实时监控页面。
- [ ] 测试 Redis 正常和不可用场景。

验收标准：

- [ ] 实时页面显示数据的实际采集时间。
- [ ] 过期数据不会显示为刚刚采集。
- [ ] Redis 不可用时系统有明确回退或错误状态。
- [ ] 异步任务不会无限制创建线程。

### 阶段 11：Netty TCP 手表协议

目标：理解设备从 TCP 报文到业务数据的完整处理过程。

建议顺序：

1. 给协议解码器编写固定字符串测试。
2. 解析一条设备登录报文。
3. 启动 Netty TCP 服务。
4. 保存 IMEI 与连接的关系。
5. 处理心跳。
6. 处理一条心率数据。
7. 接入 Redis 和数据库链路。
8. 扩展其他健康指标。
9. 处理下行命令和设备响应。
10. 最后接入模拟器。

任务：

- [ ] 学习 `WatchMessage` 数据结构。
- [ ] 学习粘包和半包问题。
- [ ] 实现 Decoder 和 Encoder。
- [ ] 实现登录、心跳和健康数据处理器。
- [ ] 记录原始报文但避免泄露敏感信息。
- [ ] 使用一台模拟设备验证。
- [ ] 验证通过后再逐步增加设备数。

验收标准：

- [ ] 解码器单元测试覆盖完整包、半包和连续包。
- [ ] 一台模拟设备能够登录和发送心跳。
- [ ] 一条心率报文能进入正确人员的健康记录。
- [ ] 无效报文不会导致 TCP 服务崩溃。
- [ ] 未启动大规模模拟器就能完成基本调试。

### 阶段 12：仪表盘、画像、报告和指挥中心

目标：在底层数据链路稳定后学习复杂聚合页面。

建议顺序：

- [ ] 工作台。
- [ ] 实时监控详情。
- [ ] 职工健康画像。
- [ ] 健康仪表盘。
- [ ] 报告中心。
- [ ] 趋势预警。
- [ ] 安全指挥中心。

实施要求：

- 聚合数据由后端计算，不用前端当前页数组冒充全量统计。
- 分页总数必须来自后端。
- 空数据要明确显示，不制造演示数据。
- 处置状态成功修改后应重新读取服务端数据。
- 尚未接入的外部呼叫、广播等功能必须显示“未配置”或“未接入”。

验收标准：

- [ ] 每张卡片都能指出后端权威数据源。
- [ ] 不同时间范围的统计含义一致。
- [ ] 空数据、部分数据和接口失败都有明确状态。
- [ ] 页面刷新后业务状态不会丢失。

### 阶段 13：AI、3D、固件与完整测试

目标：学习项目的扩展能力，并完成整体质量验收。

任务：

- [ ] 学习 AI 对话和健康报告的数据边界。
- [ ] 学习 Text2SQL 的白名单和参数化要求。
- [ ] 接入 3D 人体展示。
- [ ] 学习 ESP32-C3 固件构建。
- [ ] 使用编译宏配置 Wi-Fi、服务器地址和 IMEI。
- [ ] 验证真实设备或协议探针。
- [ ] 补充前端结构测试。
- [ ] 补充后端单元与集成测试。
- [ ] 添加 Playwright 页面测试。
- [ ] 进行构建、性能和安全检查。

验收标准：

- [ ] AI 功能不会泄露密码、Token 或个人敏感健康信息。
- [ ] 固件源码不包含真实 Wi-Fi 密码。
- [ ] 前后端生产构建成功。
- [ ] 后端测试通过。
- [ ] 主要业务链路端到端验证通过。

## 7. 推荐的前七个里程碑

开始阶段只关注以下七项：

- [x] 里程碑 1：Vue 页面成功启动。
- [x] 里程碑 2：Spring Boot 成功启动。
- [x] 里程碑 3：Vue 成功调用 `/hello`。
- [x] 里程碑 4：部门列表查询成功。
- [ ] 里程碑 5：部门新增、修改和删除成功。
- [ ] 里程碑 6：用户登录和退出成功。
- [ ] 里程碑 7：登录后可以访问部门管理，未登录不能访问。

完成这七项后，再开始人员、设备和健康指标模块。

## 8. 每日学习节奏建议

一次学习控制在 60～120 分钟：

1. 10 分钟：确定今天唯一的学习目标。
2. 20 分钟：阅读旧项目对应代码并画调用链。
3. 30～60 分钟：在新项目中实现最小版本。
4. 15 分钟：运行测试和手动验证。
5. 10 分钟：记录总结并提交 Git。

如果当天功能没有完成，保持代码仍然能够构建，不要为了赶进度继续复制更多模块。

## 9. 单个功能学习记录模板

每完成一个功能，复制下面的模板填写：

```markdown
## 功能名称

### 功能目标

这个功能解决什么问题？

### 调用链

页面 → API → Controller → Service → Mapper → 数据库

### 新增或修改的文件

- 文件 1：作用
- 文件 2：作用

### 新学到的概念

- 概念 1
- 概念 2

### 遇到的问题

- 问题现象：
- 原因：
- 解决方法：

### 验证结果

- [ ] 正常场景通过
- [ ] 空数据场景通过
- [ ] 错误场景通过
- [ ] 后端测试通过
- [ ] 前端构建通过

### 我能否独立重新实现

可以 / 还不可以

仍然不理解的内容：
```

## 10. 每阶段完成检查表

进入下一阶段前确认：

- [ ] 我能说明这个功能解决的业务问题。
- [ ] 我能画出完整调用链。
- [ ] 我能说明每个新增文件的作用。
- [ ] 我知道数据最终保存在哪里。
- [ ] 我知道正常请求和错误请求分别怎样处理。
- [ ] 后端测试通过。
- [ ] 前端构建通过。
- [ ] 手动操作验证通过。
- [ ] 没有复制构建产物或敏感配置。
- [ ] 已确认连接指定的旧数据库 `health`，且未破坏或覆盖现有业务数据。
- [ ] 已检查 `git status` 和代码差异。
- [ ] 已提交当前里程碑。

## 11. 旧项目参考入口

优先从以下文件理解项目，不要一开始阅读所有源码：

```text
health-old/README.md
health-old/AGENTS.md
health-old/HealthShow/package.json
health-old/HealthShow/src/main.ts
health-old/HealthShow/src/App.vue
health-old/HealthShow/src/router/index.js
health-old/HealthShow/src/utils/request.js
health-old/HealthData/pom.xml
health-old/HealthData/src/main/java/com/xzkj/health/HealthApplication.java
health-old/HealthData/src/main/resources/application.yml
```

查找具体功能时，按照以下顺序定位：

```text
前端路由 → 页面 → 前端 API → 后端 Controller
→ Service → Mapper → Entity/DTO → SQL 或数据库表
```

## 12. 最终完成标准

只有同时满足以下条件，才认为迁移学习完成：

- [ ] 前后端可以从干净环境安装并启动。
- [ ] 数据库迁移步骤明确且可以重复执行。
- [ ] 登录、权限、人员、设备和健康指标主链路正常。
- [ ] 实时数据、预警和手表协议有针对性测试。
- [ ] 不依赖旧目录中的构建产物或运行文件。
- [ ] 不连接旧项目数据库处理新项目流量。
- [ ] 配置中没有真实密码、Token 或 Wi-Fi 凭据。
- [ ] 能独立定位一次前端、后端和数据库问题。
- [ ] 能用自己的语言解释整个系统的数据流。

完成速度不是最重要的。项目始终可运行，并且能够解释已经实现的代码，才是本计划的核心要求。
